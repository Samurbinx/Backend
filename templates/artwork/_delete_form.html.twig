<form method="post" action="{{ path('app_artwork_delete', {'id': artwork.id}) }}" onsubmit="return confirm('¿Seguro que desea borrar esta obra?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artwork.id) }}">
    <button class="btn del">{{ ux_icon('bi:trash') }}</button>
</form>
