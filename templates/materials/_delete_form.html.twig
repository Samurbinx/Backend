<form method="post" action="{{ path('app_material_delete', {'id': material.id}) }}" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ material.id) }}">
    <button class="btn del"><i class="bi bi-trash"></i></button>
</form>
