{% extends 'base.html.twig' %}

{% block title %}Illustration{% endblock %}

{% block body %}
    <h1>Illustration</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ illustration.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ illustration.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ illustration.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ illustration.price }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ illustration.stock }}</td>
            </tr>
            <tr>
                <th>Collection</th>
                <td>{{ illustration.collection }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_illustration_index') }}">back to list</a>

    <a href="{{ path('app_illustration_edit', {'id': illustration.id}) }}">edit</a>

    {{ include('illustration/_delete_form.html.twig') }}
{% endblock %}
