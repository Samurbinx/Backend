<form method="post" action="{{ path('app_illustration_delete', {'id': illustration.id}) }}" onsubmit="return confirm('¿Seguro que desea borrar esta ilustración?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ illustration.id) }}">
    <button class="btn del">Eliminar</button>
</form>
