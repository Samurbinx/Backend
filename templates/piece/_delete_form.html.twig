<form method="post" action="{{ path('app_piece_delete', {'id': piece.id}) }}" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ piece.id) }}">
    <button class="btn del"><i class="bi bi-trash"></i></button>
</form>
